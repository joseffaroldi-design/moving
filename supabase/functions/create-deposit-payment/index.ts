import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"};
const json=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...cors,"Content-Type":"application/json","Cache-Control":"no-store"}});
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});if(req.method!=="POST")return json({error:"Method not allowed"},405);const url=Deno.env.get("SUPABASE_URL"),anon=Deno.env.get("SUPABASE_ANON_KEY"),service=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!url||!anon||!service)return json({error:"Server configuration missing"},500);const auth=req.headers.get("Authorization")??"";const uc=createClient(url,anon,{global:{headers:{Authorization:auth}},auth:{persistSession:false}});const{data:u}=await uc.auth.getUser();if(!u.user)return json({error:"Unauthorized"},401);const admin=createClient(url,service,{auth:{persistSession:false}});const{data:p}=await admin.from("profiles").select("company_id,role,is_active").eq("id",u.user.id).single();if(!p||p.is_active!==true||!p.company_id||!["owner","operations_manager","sales"].includes(p.role))return json({error:"Forbidden"},403);let body:any={};try{body=await req.json()}catch{return json({error:"Invalid JSON body"},400)};if(!body.deposit_id)return json({error:"Missing deposit_id"},400);const{data:d}=await admin.from("customer_deposits").select("company_id").eq("id",body.deposit_id).single();if(!d||d.company_id!==p.company_id)return json({error:"Deposit not found"},404);return json({error:"Legacy deposit endpoint disabled until a real payment provider flow is connected."},410);});